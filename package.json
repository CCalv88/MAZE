{
  "name": "maze",
  "version": "2.0.0",
  "private": true,
  "description": "MAZE: draw a maze, then play it in first person, solo or online with up to four players",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": { "ws": "^8.18.0" }
}
